# 视频观点提取：【Agent】为什么游戏设计师需要搭建自己的 harness？

> 来源：BV16Xbm6VEzd 口播逐字稿（同目录原始文件）。
> 提取原则：**不省略任何观点**，仅剥去故事化比喻与冗余表述。已沉淀为框架术语的比喻名（迷宫悖论 / 记忆流 / 白盒 / 认知原语等）保留其术语身份，但不复述比喻故事本身。

## 第一章 迷宫悖论与 Harness 的定义

1. **LLM 每次任务都从零出发**：它只有任务描述和训练时获得的知识，任务怎么完成全靠当次一步步走；表现随"状态"波动——这次走通，下次可能卡在中途，且昨天走通过的路今天完全不记得。
2. **迷宫悖论**：每次执行都被重置到入口，缺乏经验积累，导致表现忽好忽坏、难以稳定进步。
3. **harness 的定义**：包在大模型外面的工程层。模型本身只做"输入→输出"一件事；中间怎么走、输出怎么校验，全由 harness 决定。
4. **harness 管四类事**：①组装上下文（把记忆和知识放进输入）；②决定工具（让模型能调用外部能力）；③编排步骤（大任务拆小步、排序）；④检查输出（不合格重来）。
5. **harness 改变的是两次设置**：
   - **改变起点**（记忆/经验植入）：上次走到哪、踩过哪些坑、哪条路走对了，让下一次从离出口更近的位置起步。起点离终点越近，走到的概率越大。
   - **改变方向**（判断标准/方法体系/技能植入）：让 agent 带着判断标准选路，而不是靠直觉选路。
6. **harness 不替 agent 走路，也不替它做决定**——它只改变 agent 每次出发时的位置和方向。
7. **绝大多数 AI 系统没做到这一点**：每次都把模型扔回起点，指望它靠当次发挥走出来。

## 第二章 记忆：从静态存储到动态记忆流

8. **AI 的生命在时间上不连续**：只在推理时"活着"，输出结束状态即清零，下一次调用是全新实例。
9. **连续性问题**：聊得越多，欠下的"未完成"越多——没聊完的话题、没做完的事、答应要改的稿子，全堆在上次中断的地方，等着一个不记得它们的新实例。
10. **短期记忆 = 塞上下文窗口，有客观上限**：窗口再大（如 100 万 token）也有塞满的一天，最老的部分会被挤出去。
11. **厂商的两种做法**：自我压缩（把与当前话题无关的短期记忆压缩成文档，需要时再读）；或干脆放弃长期记忆。
12. **长期记忆怎么设计，取决于协作形态**——让 agent 陪聊还是替你干活，答案完全不一样。
13. **暴论：harness 是极度个人化、私人化的东西**。你不了解它怎么存记忆、读知识、思考、用工具，就没办法真正让它变好、没办法有效组建知识库、也理解不了它的能力边界——像雇了一个却从没看过他工作方式的员工。用不好不是它的问题，是人的问题。
14. **通用 AI 产品装的是"大多数人的平均数"**：大众需要写周报它就擅长写周报。而你要做的游戏、要写的稿子、要过的关卡恰恰不在平均数里——平均数里的答案对你往往是没用的答案（问"这个机制会不会让玩家爽"，得到四平八稳的废话）。**这是个人需要自行搭建 harness 的理由之一**。
15. **记忆的真正定义：不是存了什么，而是下次从哪出发**。存得多≠厉害；同样走三年，差的从来不是记了多少，而是下一次从哪儿出发。
16. **存储 vs 记忆流**：存储是静态囤积，存进去多少就只是多少，不会自己往前走一步；记忆流是动态的，上游的经验会自己汇入，agent 走到哪它跟到哪——不需要搬来全部历史，只需取当下要用的那部分。**存储改变你存了多少；记忆流改变你下一次从哪出发。**
17. **Nebula（作者的 harness）的实现**：
    - 每五轮设一个**检查点**：记录这五轮做了哪些决策、讨论了什么、暂时搁置了什么（定了哪几个方案、为什么否掉某个、哪条数值挂了待办）。
    - 检查点攒多了用**叙事链**串起来：不是逐字段的流水账，而是"我是怎么一步步走到这里"的纪录片——agent 回头看到的不是碎片，而是一条讲得通的来路：知道昨天为什么做那个决定，也知道今天从哪接着走。
    - 原始数据绑在链上，将来要查验可以一路溯源。
18. **检索必须双轨**：BM25（字面匹配，精准但认不出同义词）+ 向量（语义匹配，模糊但可能召回不准）。两者互补才不漏——一个负责"我清楚地记得"，一个负责"好像在哪儿见过"。

## 第三章 知识：从知识库到可推理的方法体系

19. **堆砌知识库走不通**："知道"不等于"会做"。知识是关于世界的陈述，会做是对世界的动作——中间隔着巨大的距离。问什么都懂，让它做就傻眼。
20. **知识的真正定义不是信息量，而是方向感**：在岔路口知道哪个方向更对。**给 agent 一万条知识，不如给它一条判断标准**——知识是散的，判断标准是活的：知识告诉你世界上有这么回事，判断标准告诉你此刻该往哪走。
21. **老手与新手的差别在判断标准**：这套标准不是从书上抄的，是在一次次做、一次次错、一次次复盘里长出来的。
22. **知识库的三个死穴**：
    - **存了≠用了**：知识库是静态的，agent 不知道此刻该用哪一条；条目多到一定程度，反而一条都不想读。
    - **知道≠会做**：只解决"关于世界的陈述"，不解决"对世界的动作"——会点评，不会动手。
    - **平均数的方向感**：装的是大多数人的判断，而真正有价值的判断恰恰在大多数人不会多看一眼的地方；平均数只会把你带向平庸。
23. **解法三步**：从知识里提炼方法体系 → 把方法消化成"会做"（技能）→ 把技能拆成可自由排列的原子。知识库本身没有价值（论文说 A、书说 B，不知信哪条）；有价值的是从中提炼出的分析方法体系。
24. **方法体系由三块构成**（知识骨架）：
    - **游戏骨架**：这个机制会产生什么效果。
    - **虚构的人**：玩家会怎样体验。
    - **设计哲学**：这符合我们相信的东西吗。
25. **体系优于知识库的三个理由**：
    - **可推理**：给定条件能推出该怎么做、会有什么效果。知识是死的，体系是活的。
    - **可质疑**：一条知识错了很难发现（孤零零无人查）；体系里哪一环逻辑站不住，一眼看得出，它逼着你把每一环都过一遍。
    - **可分析**：知识是散乱的，你不知道自己缺什么；体系是一张清单，哪个角落没覆盖、哪类场景还没想，一目了然。体系比知识库强，不是因为它知道得更多，而是它能让你发现自己哪里还不知道。
26. **可推理的实例（归因引导判据的推导链）**：
    - 玩家失败的第一反应是甩锅（"数值有问题""这关恶心人"）——这是人类通用的自我服务偏差：成功算我的，失败怪别人。
    - 维纳归因理论把"找原因"拆成三个问题：这事儿怪得着我吗？我能改吗？是暂时的吗？
    - 三种走向：怪我 + 能改 + 暂时 → "我下次能行"（学习动机，最好的状态）；怪我 + 改不了 + 一直如此 → 习得性无助（躺平）；不怪我 + 我无能为力 → "这破游戏针对我"（愤怒弃坑）。只有第一种是设计要的。
    - 人为什么甩锅：承认"我输了"等于承认"我菜"，伤自尊；越让玩家觉得"是我没做好"，他越要躲。**关键在自尊**。
    - **最伤自尊的是损失**（代价太大难承认，损失越大锅甩越狠）；落差则不伤自尊（"我差一点就过了"→"再试一次就能行"，恰是第一种归因）。
    - **结论（判据）：设计师应当让失败当信号，它不能也不该是惩罚。移除损失比移除落差更重要——落差是信号，损失是干扰。** 设计动作：与其纠结要不要惩罚，不如想怎么让这次失败不伤自尊；与其让玩家死一次掉光装备，不如让他立刻重来再试一次。
    - 整个过程是从理论推出来的，每一步踩在真实心理学实验上——这就是"知识变成体系"的好处。
27. **方法最终要消化成本能**：不用每次设计都从零推理一遍，上手就是那套动作。

## 第四章 技能原子与认知原语

28. **技能的致命问题：太固定**。写死的招式换个场景就废；设计里最要命的是没有固化的顺序——今天这个关卡要考虑的东西，明天换个游戏就可能用不上；没有任何一个流程能写死套在所有游戏设计上（平台跳跃的流程套不到自走棋上）。
29. **技能原子的定义**：可以自由排列的基本动作。任务不同，搭法不同，怎么排由你说了算。
30. **拆解的另一个原因：上下文挤占**。主流做法"渐进式披露"（先给一行鸟瞰，agent 觉得合理再调工具读完整内容，用一次 API 调用换上下文空间）在游戏设计场景失效：技能太多，几十个技能的鸟瞰说明叠起来也是大开销，而且 agent 可能打开多个技能、来回多次调用 API，又贵又挤占。
31. **正确的拆法：按认知原语拆**——AI 做任何一件事（写文章/做设计/修 bug）底层都要用到的那几个基本动作。
32. **十个认知原语**：
    1. **模式识别**：看见规律（100 条评论看出 80% 差评集中在第三章）。所有理解的起点——看不见规律，后面一切无从谈起。
    2. **归纳概括**：从具体到抽象（100 条零散反馈归纳成 3 条核心问题；2 万字压成 3 句话）。没有它，AI 永远停在"我见过这个案例"，不会说"这类案例都这样"。
    3. **分层抽象**：同一件事站在不同高度看（世界观/玩法/数值/关卡各是一层）。能分层才不会把复杂问题一锅粥处理，才能把大问题拆成能下手的子问题。
    4. **因果推理**：从"发生了什么"推出"为什么发生"（玩家流失：引导问题？难度问题？内容消耗太快？）。从看见现象到理解世界的桥。
    5. **约束求解**：在限制条件下找答案（三天后上线、人手只有两个）。真实世界全是限制，没有限制的任务根本不存在；有了它 AI 才不会天马行空给建议。
    6. **逆向推理**：从目标倒推（三个月做完游戏 → 提前两个月备宣传素材 → 提前若干月要有版本）。从"要去哪儿"反推每一步怎么排。
    7. **假设检验**：提一个猜想然后验证它。光有感觉不行，得把感觉变成可以验证的猜想。
    8. **序列编排**：把动作排成正确顺序（哪个先做哪个后做、哪个能并行、哪个必须等前一步）。
    9. **类比映射**：把一件事的方法搬到另一件事上（会游泳学潜水快——底层逻辑是通的）。
    10. **心理模拟**：在脑子里先演一遍还不存在的东西（关卡还没做，先闭眼演：玩家从哪进来、先看到什么、会往哪走、在哪卡住、通关时什么表情）。**前九个处理的都是已存在的信息（看懂世界）；第十个面对的是还不存在的东西（想象世界）。**
33. **铁规矩：一个技能原子只能挂一个认知原语，一个原子绝不干两件事**——确保每个原子都是最小单位。砖越小，拼法越多。
34. **原子的两种使用场景，注入方式完全不同**：
    - **日常对话（原子可选）**：预注入 + ReAct 间注入。预注入——话题漂移到哪、任务走到哪个阶段、当前情绪是什么，多个检测器各打分，由多路加权路由综合判定后把对应原子补进上下文；ReAct 间注入——捕捉模型输出里的思考内容，在每次调用间隙动态补原子（聊到判断价值→注入检验类原子；聊到机制创新→注入类比映射）。好处：上下文里永远只有当下相关的原子，而不是背上整个工具箱。
    - **正式设计（原子必定要走）**：先梳理**原子清单**（列出本次设计任务可能用到的所有技能原子，手动或让日常 agent 来做）；再安排**原子执行表**（第一步必定用谁、某一步可在发散池随机让 agent 选），按顺序一一执行。流程显式（如：第一次调用写初稿 → 第二次拿原子做攻击性检查 → 第三次收尾补遗漏），每一步有明确产出可检查，每一步踩在哪颗原子上清单里写得清清楚楚。
35. **设计场景的原子编排实例（自走棋 PVE 单位设计）**：概念步——模式识别 + 视角置换（找这个机制想表达的体验还缺哪个设计词汇）；逆向推理 + 制造喜欢（围绕"玩家喜欢上它的那个瞬间"倒推：瞬间从哪来、什么时候兑现）；约束求解 + 留白设计（别把探索空间一次填满，答案全写死玩家就没东西可发现）；预检步——模式识别 + 极端化推演（数值拉满会怎样、会不会破坏平衡、压掉别的选择）；设计步——随机两颗收敛性原子；收尾——约束求解 + 减法判断（逐个单元问"这个效果删掉快乐会不会塌"，塌了的留下、不塌的删掉）。
36. **原子可观测 → 可修调**：每一步用哪颗原子做了什么都是显式的，因此可以拿中间产物判断每颗原子起了多大作用（正面/负面影响各多少），据此修调——哪颗原子老带偏就修哪颗，哪颗老立功就多用哪颗。
37. **agent 分工：核心 agent 与设计 agent 分开**。
    - 核心 agent（作者的"悠悠"）负责三件事：管理记忆沉淀与知识/文件操作；理论推导与论证（把想法推到底）；实际讨论（陪聊把一件事聊清楚）。它带着整个工作区的历史脉络（上周聊到哪、三天前定过什么规则、昨天哪个方案被否），靠的是模拟的情感效价/唤醒度 + 预注入的叙事链信息。
    - **设计恰恰不能让核心 agent 做，两个原因**：①它的上下文对设计全是噪音（昨天聊了什么、哪个文件改过、哪些共识——会把设计往历史惯性上带，而不是往当下判断上带；**设计要的是现在这个任务该怎么想，不是我们以前是怎么想的**；设计者要能掌控 agent 获得的全部信息：它看到什么、按什么规则走、产出什么格式，都知道）；②设计需要尽可能的白盒。
    - 设计 agent：不带历史包袱，只带设计要用的那套白盒骨架，所有 prompt 围绕"专做设计"这一件事。

## 第五章 白盒化工程

38. **LLM 是黑盒，这是工程结构决定的**：前向计算是权重矩阵运算，中间没有步骤；就算导出每层激活值，也不是人能读懂的推理过程；模型输出的"思考过程"文字是它生成的，不是思维日志，不一定忠实——它说"我这么想"不代表它真这么想。**使用者能确定的只有两件事：进去了什么、出来了什么。**
39. **黑盒本身不可怕，可怕的是让黑盒做设计**：设计决策必须可推理、可质疑、可审计、可迭代。设计要复盘——这个机制为什么有正反馈、那个数值为什么导致崩坏——必须能一路拆到是哪一步判断错了；拆得动才能改、才能进步。游戏设计就是一个发现问题、解决问题的专业。
40. **白盒化不是改造模型**（内部权重永远打不开），而是包在模型外面的那一层透明工程层：流程怎么走、每一步依据什么、产出长什么样、什么时候停下来检查，全摆在明面上。**黑盒出想法，白盒管轨道**——想法还是 AI 想，但每一步踩在哪、为什么这么走、走得对不对，全都看得见。
41. **全黑盒 = 不可解释 → 不可迭代、不可复盘、不可教**，永远停在"发挥"层面（回到迷宫悖论）。全白盒（全人工手搓）这条路不存在。
42. **白盒骨架五条原理**：
    1. **流程显式**：分阶段走（如概念→初稿→攻击性检查修改→减法设计落定），每步有明确产出、可检查、可追溯。
    2. **判断有依据**：每个决策挂得住判据，能回答"为什么这么设计"，而不是"感觉应该这样"。
    3. **产出固定格式**：可审计、可比较、可回退；格式乱了比不了，比不了就改不动。
    4. **检查点**：每走一步存一次档，错了能退回，不推倒重来。
    5. **回顾性自检**：设计完成后回头检查一遍，而不是在设计过程中空想。
43. **白盒真正的价值不只是"看得见"，而是能度量三件事**：
    - **实际检验上下文的影响**：每个设计决策能追溯——是上下文里哪条东西在起作用、哪条判据哪颗原子影响了它，影响路径可见。纯黑盒出了烂设计，分不清是上下文的问题、模型的问题还是哪个环节的问题；白盒能精确到"就是这条判据在带偏"。
    - **事后评分**：设计完成后按知识骨架评分（好不好、好在哪、差在哪），分数挂回这次设计的过程记录——得到一条能比、能累积的设计质量标尺，不凭感觉，数字说话（这个月比上个月强了还是弱了）。
    - **观察技能原子的使用情况**：这次设计用了哪颗、用得对不对、哪颗反复被用却总是带偏、哪颗该用却没用上。原子是白盒里最小的可观测单元，用没用、用对没，一目了然。
44. **三件事合起来是反馈闭环**：精确定位上下文里影响设计质量的问题点 → 修正它 → 下次设计变得更好。
45. **副产品：设计可以批量化**。卡牌/装备/武器这类大规模内容，以前靠人肉一个个列维度、找中间层、检查质量；白盒流程固定——同一套骨架、同一套评分、同一个修正回路——从跑一个设计到跑一万个设计，越做越准。**白盒把设计从手工作坊变成流水线**（作者认为这会深远影响一些游戏门类）。

## 第六章 上下文腐烂治理与 Task 机制

46. **上下文会腐烂**：不是越积越厚、agent 越懂，而是越积越烂——过时的记忆、与当下任务打架的旧结论、用不上的细节、半年前的决定、已经改掉的规则、早已废弃的方案全堆在里面，关键信息被稀释，agent 表现悄悄下滑。你以为它变笨了，其实是上下文烂了。
47. 渐进式披露的坑也是一种腐烂（被技能说明文字泡烂）；技能原子按需注入 + 限制个数解决了技能这一块，但技能只是上下文的一部分——人格、知识、历史、系统状态同样需要治理。
48. **答案：不是所有场景用同一套上下文，上下文按 task 划分**。task = agent 现在要干什么（游戏设计/前端开发/文档审查/自由讨论），不同 task 该注入的模块本就不一样（例：游戏设计不需要知道工作区原况、磁盘状态、系统在跑什么——注入进去全是噪音，只会把设计思路带偏）。
49. **task 不是标签，是一整套规则**：决定 agent 这一轮长什么样、影响它的上下文都有哪些东西。
50. **task 机制三步**：
    1. **检测**：对话进来先判定 task（靠关键词：听到游戏机制/心流/平衡→游戏设计；听到前端组件→前端开发；匹配不上→自由讨论）。
    2. **注入**：上下文构造器按 task 过滤层次（游戏设计注入知识体系/设计哲学；前端注入前端规范/前端库；闲聊注入最近新闻/新发售游戏简介）；工具分级暴露（主工具/次工具/补充工具——游戏设计 task 的主工具是搜索/读文件/检索知识库/调用设计 agent；用不到的工具根本不给，给了也是干扰）；原子池按 task 装配（游戏设计 task 里就没有写稿类原子，如"空话套话书面墙识别"）。
    3. **效果**：同一个 agent，判成游戏设计就带设计骨架/知识/工具/原子，判成前端就带前端那套——工具、上下文、思考方式全部跟着 task 走。
51. **为什么不做多个 agent 各管一块**：①人做游戏设计时考虑的东西做饭时不考虑，但人还是同一个人——聊到一半切 agent 换窗口太打断思路；②每个职责只安排一个 agent，那个职责的工作容易陷入局部。（作者留作下一期话题。）
52. **上下文的模块清单**（每块存在的理由都可以用一句话回答："缺了它，agent 会在某一类问题上变瞎、变聋、变傻"）：
    - **你是谁（agent 人格）**：必须先知道自己的身份——协作伙伴，不是万能应答机。人格决定语气、立场、协作方式；没有它就是个谁都能使唤的问答工具。
    - **对方是谁（用户画像）**：知道在跟谁协作——偏好、工作习惯、设计立场（极简还是华丽、直接还是委婉、上次否掉方案是嫌保守还是嫌冒险）。不知道就只能瞎猜，猜错一次多生一次气。
    - **怎么做（行为手册/系统提示）**：怎么用工具、怎么回应、什么能做什么不能做。没有它就没有行为约束。
    - **当前环境原况（现场）**：现在几点、工作区什么状态、有几台电脑可调度、各装了哪些功能。不是所有场景都需要——游戏设计就不需要磁盘状态。
    - **系统结构（地图）**：工作区地图、资料库、知识库、原子库在哪。没有地图 agent 不知道去哪儿找东西，只能瞎转。
    - **我们信什么（不变的立场与裁决标准）**：方向取舍时的最终裁判（例："机制要为体验服务"——当"这个机制很炫"和"这个机制让玩家迷茫"撞上时，炫的让路）。没有它，agent 今天往东明天往西，因为没有一条不能弯的底线。
    - **对话历史 / 叙事链**：聊到哪儿了——连续性的来源、记忆的落地。但它也最容易腐烂，所以要收敛，防过时的旧结论干扰当下判断。
    - **相关通用共识检索 top3**：工作弹药——按当前话题精准检索用得到的共识，而不是全库塞入（聊到负反馈时，"归因引导"那条共识自己浮上来），避免 agent 每次再问一遍。
    - **技能原子**：思考的工具箱，按 task 和思考时刻装配。
    - （另提及：有一个专门的 agent 负责收集信息和内容。）
53. **task 机制 = 给上下文做白盒治理**：记忆流防的是**起点腐烂**（每次出发都从干净的位置开始）；task 机制 + 上下文构造器防的是**上下文腐烂**（每轮看到的都是这个场景该看的，而不是历史积攒的全部）。
54. **harness 的本质不是帮 AI 多记，而是帮它每次少带一点**——少带腐烂的、过时的、用不上的，只带此刻真正需要的。这才是让 agent 越来越懂你的方式。

## 结尾 为什么必须个人化（完整理由）

55. **记忆是你的**：什么值得记住只有你知道——哪场讨论改变了方向、哪个决定推翻了从前。
56. **知识是你的**：什么方向对只有你能定义——别人说的正确，不一定是你的正确。
57. **判断是你的**：快乐总量、心流、失败成本这些词听着抽象，但每一个都是从你自己的设计里长出来的。
58. **方法是你的**：一套体系、一组原子、一条白盒流水线，装的全是你的判据。别人拿去，装的是你的答案，做不了你的活。
59. **通用 harness 装的是大多数人的平均数，而你要做的设计、要走的路恰恰不在平均数里。**
60. **生产力的意义**：以前因为时间和生产力做不了、只能搁置的东西（一个人慢慢磨的玩法、需要批量验证的设计、被丢在角落的构想）现在都能拿起来做了；还有新方向——把 agent 真正塞进游戏（不是粗浅的历史模拟，而是用 harness 工程影响游戏内人物、甚至变成玩法）；另有大量原有内容可以开采。
61. **agent 从来不只是工具**：工具是用完就放下的东西；agent 是自我意识的延伸——记着我记过的东西，带着我的判断标准，用我的方式思考，替我把"如果是我，我会怎么想"跑一遍。
62. **搭 harness = 把自己搬进一个能一直活着的容器**：记忆在里面流，判断在里面增长，思考方式被拆成一张张可以自由排列的卡片——它替你一遍一遍走过你走过的路，每一步都比上一次离出口更近。
63. **收束句**：别人给得了你地图，给不了你要去的地方。
