# 框架升级开发计划（多阶段）

> **定位**：2026-08-24 框架升级 run 的执行计划。依据 = 待办重排（A/B/C 档）+ 近几轮讨论定案：
> ①流程动沉淀→模板动作链→新项目继承（防"换新项目靠碰"）；②三实体职责+使用时刻表（理解=骨架/判断=判据/动作=原子）；
> ③PACK=内容层生长物、编排=建议非权威；④目标形态=自动运作（机器做机械层、你只做设计裁决+圈选升级）；
> ⑤红线：不膨胀不冗余（面板=甜品不做；已有机制不搬）；红线 7（观察先确认再普适化）；状态在文件（每阶段落盘+提交）。
> **原则**：先做继承链断点（A 档）→ 再做制度壳（B 档）→ 其余全部"按需长出"，禁止批量预做。

## 阶段 0 · 基线核实（前置，半天内）

- **目的**：不开盲工——调查决定阶段 1/2 的实现方案。
- **内容**：
  1. DSH 第二协议注入机制调查（workspace 指令是否支持多协议文件 / HARNESS-PROTOCOL 自动注入可行性）→ 决定 M3' 分支 A（自动注入）或分支 B（preset 契约化）；
  2. writer.ts 协议模板现状核对（已含条款清单 vs 需补：§3.7-3.10 / 交流规范 / 内容生命周期分层 / 切片纪律）；
  3. 插件 build/装配链路确认（dev_build_plugin → 热载/装配验证要点）。
- **产出**：`docs/DEVELOPMENT-PLAN.md` 更新（方案定稿）；结论入 PROGRESS。
- **验收**：两个分支点有事实依据，无猜测。

## 阶段 1 · M1′ 动作链模板化（继承链核心，2-4h）✅ 2026-08-24 完成

- **目标**：本次 run 已沉淀并获用户确认的动作链，全部进入新项目继承链（今天接新项目即完整版）。
- **内容**：
  a. 插件 `writer.ts` 协议模板同步：§3.7 挂载前验真 / §3.8 骨架留痕 / §3.9 决策时挂载 / **§3.10 原子执行记录** / §3.11 写作纪律（+§3.6 harness-dev 对齐）；
  b. 协议模板补：**交流规范条款**（同事式/选择题+自定义/切片反馈/"已记录"确认——默认条款，项目可覆盖）+ **内容生命周期分层**（run=簿记 / 脑暴方案=候选 / 转正提升设计域 / 决策=叙事链）；
  c. **切片写作纪律**（因果自洽先于结构）升为协议 §3.11 通用底线；
  d. **叙事链分层适配（C-06）**：reader/_MAIN.md + 项目链双源（readNarrative/readProjectNarrative）、run_start 注入双源标来源、run_writeback 项目 run 写项目链；
  e. **骨架注入按需裁剪**：PACK 引用优先（[pack]）+ 每块上限 4 + 总量上限 14（[block]）——修复"全量 63 条=上下文过载+术语榜样"（注入即起点治理）。
- **产出/验收**：冒烟脚本 `scripts/smoke-phase1.mjs` **19/19 PASS**（f527e7c + 4c77136，feature/framework-upgrade 分支）；ADAPTER 契约同步（2026-08-24 版本联动条）。

## 阶段 2 · M3′ 会话启动链（继承链第一环）✅ 2026-08-24 完成（同日修订 A2′）

- **目标**：新会话"进得来、记得读协议"——不靠用户记得选预设。
- **阶段 0 判定（核实结论）**：DSH **支持多协议注入——分支 A 成立**。`@deepseek-ai/dsh-agent-instructions`（active）`instructionFileCandidates` 可配置（默认 `["AGENTS.md","CLAUDE.md"]`），按 项目根→cwd 目录链逐目录发现、多段注入、带字节预算与截断；默认预设 `standard` 经 standing scope 挂载该插件 → AGENTS.md 早已对所有会话自动注入（框架工作区实证：router-standard 会话亦注入，无需 harness-project 预设）。**缺口唯一**：候选清单未含 `HARNESS-PROTOCOL.md`——星尘叙事者项目默认会话只注入 Maker 托管 AGENTS.md（55.4KB 纯引擎政策）+ CLAUDE.md（81B），设计协议完全靠自发现。
- **分支 B**（preset 契约化）：**未选**。分支 A 成立后不再必要；harness-project 预设保留为"选对时"条件解（persona 铁律+工具面），与自动绑定（无条件解）互补（见 ADR）。
- **实施（A2′ 工作区感知自动绑定；历经用户纠偏：A2 全局默认方案被否——框架预设不该污染非框架项目默认）**：
  a. `harness-project` 预设 `agent-instructions` 行补候选 `[AGENTS.md, CLAUDE.md, HARNESS-PROTOCOL.md]` + `maxBytes: 98304`（选中路径与自动绑定路径共用）；
  b. **host 钩子**（插件 `src/host/session-bootstrap.ts`）：`agent/created` → 会话 cwd 项目根含 `.gamedev-harness.json`（接入标记）+ 当前预设=部署默认 + blank 窗口 → `recompose('harness-project')` + 写 `agent-preset/selected` 事件（模型可见⟺日志诚实，同宿主 RPC 写法）；用户显式选择的其他预设一律尊重；
  c. **默认预设保持 `standard`**（平台中立，未接入项目零感知）；A2 方案的 `harness-standard` 副本预设废弃删除；
  d. 冒烟 `scripts/smoke-phase2.mjs` **22/22 PASS**（框架工作区回归 / 修复前后对比=缺口实锤 / 截断保末段语义 / 默认中立+harness-project 候选 / 绑定纯逻辑：标记判定+绑定条件矩阵）；
  e. ADR `2026-08-24-m3-session-startup-chain.md`（含同日修订注记）。
- **已知限制**：①`router-standard` 等实验预设显式选中时保持原样（红线 7：未点名不普适化——遇用再评）；②钩子只作用于新会话创建（`agent/created`），已有会话不迁移；③DSH 升级后 harness-project 组合若失配 → 重跑 smoke 对账；④FRAMEWORK-MIND.md 保持"协议内读"不注入（完整段/不得跳设计不变）；⑤插件已热重载 + 运行中默认本就 `standard` → **无需重启即生效**。
- **验收**：模拟 = smoke-phase2 22/22；用户首次实测 = 在星尘叙事者开新会话（默认预设、不显式选择）——会话自动绑定 harness-project（日志含 `agent-preset/selected` 事件）且工作区指令含 `HARNESS-PROTOCOL.md` 段；非框架项目新会话保持 `standard`、无感。

## 阶段 3 · 流程生长轨（动态沉淀的制度壳）✅ 2026-08-24 完成

- **目标**：让"流程在 run 中诞生→圈选→模板化"成为默认习惯，升级裁决有固定接口。
- **内容**：
  a. **协议「流程观察」段**：协议 **§3.12 流程观察与圈选**（writer 模板 + 星尘叙事者协议；§3.11 写作纪律同步补齐——阶段 1 模板同步遗留）——当场一行（格式 `流程观察：<现象>｜<类型：摩擦/失败/被纠正/有效模式>`），只记事实不加工、不普适化、不判责；**流水线回顾段说明**（_FORMAT + card-batch 阶段 5 + incubation 阶段 4：圈选动作）；
  b. **run 结束圈选流程**（随 05-review：观察清单化 → 用户逐条 沉淀/弃/改 → 沉淀项过验收三问 → 圈选结果回写叙事链）；
  c. **沉淀验收三问**：①落哪条模板动作链？②新项目继承链哪一环？③答不出=沉淀未完成（写入 §3.12 条文）；
  d. **首案例回填**：星尘叙事者 9 项沉淀清单 → `docs/FLOW-OBSERVATION-CASE-01.md`（00-brief 复盘素材段枚举恰 9 项；8 项=阶段 0-2 已裁决沉淀+标注落位；**#9 代价提醒=现场演示点，已于 2026-08-24 圈选为沉淀 → 判据候选 `unlit-not-dead`（[候选]）**）。
- **产出**：协议 §3.12（流程观察与圈选）+ 流水线回顾段说明（3 处）+ 首案例回填；smoke-phase1 **21/21 PASS**（新增 2 断言：§3.12/类型枚举+格式）；ADR `2026-08-24-framework-upgrade-phase3-flow-observation.md`。
- **编号注记**：计划原文「协议 §3.11（流程观察与圈选）」与阶段 1 已固化的 §3.11 写作纪律序号冲突——实际落位 §3.12（写作纪律=§3.11 保持不动）。
- **验收**：下个 run 产出含流程观察段（机制已入协议义务，不靠自觉）；圈选走通一次 ✅（首案例 #9 于 2026-08-24 圈选为沉淀 → 判据候选落库——框架升级 run 本身即首个真实圈选现场）。

## 阶段 4 · M4 观测联动（规范件 ✅ 2026-08-24；验收随 A-01 接续）

- **目标**：05-review 从 §3.10 执行记录**聚合**原子使用表（禁回忆回填）→ 3 验计数路径通。
- **内容（规范件已落盘 2026-08-24）**：流水线回顾段注明数据来源（执行记录→观测表）——`_FORMAT` / card-batch 阶段 5 / incubation 阶段 4；候选 3 验计数规则标注（机制文档层）——GOVERNANCE 时刻表环节 5/7（计数来源=执行记录聚合，无执行记录不计）。
- **依赖**：A-01 接续（用户暂停中）；**工具化延到 run 实际跑起来再定型**（不预先做——观测表/自动聚合为阶段 5 候选）。
- **验收（待 A-01）**：星尘叙事者续跑阶段 3 时，05-review 有执行记录来源可对账——规范件保证来源标注就位；实际对账在续跑时进行。

## 阶段 5 · 按需长出（无排期，逐项随需求）

- 挂载验真工具定型（run 用到时）；C-03 注入补全（用户画像/共识 top3）；C-06/F-03 检索小改；
  PACK 定位注记（`_TEMPLATE` 补"编排=建议非权威、内容层生长物"）。
- **原则**：每项都等真实 run 长出需求或用户点名——**禁止批量预做**。
- 验收：逐项按需。

## 明确不做（防膨胀防线）

- 面板任何"操作台/观看"功能（甜品判定，AGENTS §6）；
- 落库机检搬进插件（G-20 脚本已做且验证，搬=冗余）；
- checkpoint 产出格式校验（需求未长）；
- 向量轨 C-01（mnemon 宿主故障 + 无活跃需求，双条件挂起）；
- C-04 设计型双维、任何"先设计等 run 验证"的规范。

## 执行方式

- 每阶段完成：落盘（PROGRESS/文档）+ 双仓提交（框架仓 push / $HARNESS 本地；项目仓按惯例）；
- 阶段间衔接：A 档（1-2）优先级最高——它们直接兑现"换新项目自动继承"与"零操作会话"；3 之后随真实 run 节奏推进；
- 串行不并行：本计划全程一个工作区（框架核心区）执行，项目 run 不受干扰。
