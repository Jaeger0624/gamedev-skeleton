# 框架心智基线（FRAMEWORK-MIND）

> **启动必读完整段**（2026-08-24 用户定案：解决"新会话忘记架构精神"）。任何会话启动按序：①本文件 → ②`AGENTS.md`（运行时协议）→ ③按任务数据入口恢复（书地图/book.json 等）→ ④本轨规范（reading→READING-PROTOCOL §1+§3.3；涉及原子判定→GOVERNANCE）。
> 本文件=**模式层**（架构精神，可随框架迁移）；内容层（判据/原子/骨架内容）在全局骨架 `$HARNESS`；实体定义详见 `atoms/GOVERNANCE.md` / `HARNESS.md` / `verdicts/_FORMAT.md`。

## 一句话：harness 是什么

不替模型思考——只改变其出发时的**起点**（记忆/上下文）与**方向**（判据/方法体系/原子编排）。（迷宫悖论）

## 白盒五原理（与 HARNESS.md 同源，视频 42 条原文）

1. **流程显式**——分阶段走，每步有明确产出、可检查、可追溯
2. **判断有依据**——每个决策挂得住判据，能回答"为什么这么设计"，而不是"感觉应该这样"
3. **产出固定格式**——可审计、可比较、可回退；格式乱了比不了，比不了就改不动
4. **检查点**——每走一步存一次档，错了能退回，不推倒重来
5. **回顾性自检**——完成后回头检查，而不是设计过程中空想

> 白盒的**价值**（视频 43 条，度量层）：①每个决策能追溯（哪条判据/哪颗原子在起作用）②按知识骨架事后评分（分数挂回过程记录）③观察技能原子使用（用没用/用对没/哪颗带偏）——三件事合起来=反馈闭环（定位问题→修正→下次更好）。

## 三实体职责（判定前先问：这属于谁？）

| 实体 | 回答 | 判定问句 |
|---|---|---|
| 骨架 | 是什么/为什么（知识陈述） | 它让 agent **理解**？→ 骨架 |
| 判据 | 应该怎样/该往哪走（标准） | 缺失的是**判断**？→ 判据 |
| 原子 | 具体**执行手册**（"当你 X 时，执行 N 步"：输入/步骤/观测信号） | 缺失的是**动作指令**？→ 原子 |

- 同源可**双投影**：一条观点可同时投影骨架（原理）与原子（执行），两投影各自独立文件、互不替代（如 `indirect-control` 骨架=三方法总纲 ↔ 原子=执行应用步骤）。
- **查表式非原子**：对照清单/枚举应用（=使用判据或骨架本身）无独立操作序列，不立原子。
- **禁止按名字判定职责**——同一名称可能职责不同（"单变量"=判据设计问句 vs 原子测试纪律）；职责判定依据**执行文本**。

## 红线（追加式：每次实战教训/用户纠正固化于此，带来源+日期）

1. **技能原子 = agent 的具体执行手册**——无执行文本（输入/步骤/观测信号）不登记；只有名称=信息不足，不算有效候选（2026-08-24 用户定案）
2. **禁止按名字判定职责**——职责判定依据执行文本，名字相似≠同职责（2026-08-24，035 误判教训）
3. **白盒 = 来路可见 + 断点可定位**——回看必须能答"这条从哪来（轮/节/L1/L2 衍生）、在哪个环节断的"；数据状态与规范状态都要有恢复入口（2026-08-24 用户强调）
4. **积累不走偏**——候选登记前四维预检/执行手册判定，不合格不登记（降级/合并/转类）；零默认轮定期强制（防 status quo bias）；全量回搜必须三线交叉，禁止单线定论（2026-08-24 用户定案）
5. **不确定就问用户，不猜**——task 判错会污染整条流水线；判定依据拿不准时先问（2026-08-24 用户定案）

## 启动自检（防退化，AGENTS.md 铁律）

能一句话说出三实体区别？本次判定依据是文本还是名字？——说不出=启动未完成，回头补读。

## 生长规则

- 只生长**红线级**约束（教训/纠偏），不生长内容（内容归骨架/判据/原子）；
- 追加带来源+日期；变更走 conventional commits + 叙事链回写。
