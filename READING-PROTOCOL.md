# READING-PROTOCOL — 读书学习方法（框架模式）

> **定位**：框架新生长的"读书"学习法——从文本/文章/讲座/书中摄取具体内容，经用户取舍判断后沉淀进全局骨架。与"项目复盘"并列的第二轨：**不依赖真实项目**。
> **思想对齐**（框架五原理/视频观点）：白盒（每步可见可审计）、判断有依据（你的圈选是最终裁决）、不堆知识库（有取舍、有查重、有来源）、上下文防腐（每轮只带当前节）、记忆流（每轮入账可追溯）。
> **与 E/F 组的关系**：本模式 = 执行手册；E 组（knowledge-sinking）= 单次非书输入的沉淀流程；F 组 = 参考书库（存/检/引）；G 组 = 待办清单。范围差异见 §1。

## 1. 适用范围（输入形态 → 处理方式）

| 输入形态 | 处理方式 | 状态对象 |
|---|---|---|
| **书**（厚/多章） | 书级状态机 + 逐节阅读轮（多会话） | `books/<slug>/book.json`（状态/游标/节状态）+ `读书日志.md` |
| **文章/讲座稿/视频文字稿**（中等，单次） | 一次性阅读轮（单会话内的提取→看法→判断→沉淀） | `sources/<slug>.md` 登记 + 轮记录入档（可并入 run 存档） |
| 片段/短观点（用户随手贴） | 轻量轮：直接走"判断 → 沉淀"（提取已由用户给） | sources/ 登记 |

> 统一核心 = **阅读轮**（§3）；差异只在状态对象的持久化程度（书要跨会话，短文不需要）。

**产物三通道分流（克制原则，2026-08-23 确立）**：描述/原理 → 骨架；标准/该往哪走 → 判据；可执行动作 → 原子——**原子是慢通道**：书里拆出的动作候选只登记（记入 section `yield.insights` / 读书日志），须在真实设计 run 中被使用并观测效果后，才可走 propose（四维审计 + 3-run 验证通道）。**不为平衡产出硬拆原子**（伪原子违反最小性/正交审计）。
> **原子候选预检（2026-08-23 用户裁决并修正）**：判"可观测"时，**分界不在"是否涉及感受"，而在两种感受的区分**——
> - **玩家体验的心理模拟**（数值→玩家感受、关卡→体验、挑战→状态；基于模型 + 可验证信号）→ ✅ 可进候选（`mental-simulation` 原语能力域；弹性挑战/深度测量/关卡设计均属此类）；
> - **执行者自身的感官/品味体验**（"我听了觉得怪""我感受到了氛围"——agent 无感官体验，评判即编造）→ ❌ 不立为原子候选；
> 口诀：**"推演玩家的体验"可以，"凭自己的感受说话"不行**。
>
> **① 原子候选四维预检前置（2026-08-24 用户定案，防积累走偏）**：候选**登记前必须先过四维预检**（GOVERNANCE 四维：铁律/最小性/可观测/正交），预检表附在圈选清单③；预检不合格 → **不登记**（降级：被判据/骨架承载 → 并入现有条目；类型错位 → 转判据；未成型 → 观察项；同族 → 合并为一个）。合格者才"仅记录（待 run 验证）"。
> 判定口诀：**技能原子 = agent 可执行的认知操作**——有操作指引步骤、效果有明确观测信号；**生活建议/习惯（如"随手记灵感"）、实例集（如"×案例集"）、骨架已载枚举（如"×方法清单"）不是原子**，一律不登记。
> **执行描述=候选前置必录项（2026-08-24 用户定案）**：技能原子是 agent 的**具体执行手册**——候选登记必须包含**一句话操作指引**（"当你 X 时，执行 N 步：…"结构，含输入/步骤/观测信号）；**只有名称或一句描述、无执行文本 = 信息不足，不算有效候选**，不得进入预检（先补执行描述；补不出=它本就不是原子，转观察）。
> 反面教训：32 条历史候选多为"名字式"登记，导致预检只能按名字相似度误判（035"单变量观察法"与判据 `single-variable-device` 名字像就被宣判同义——实际其动作面成立，应并入观察测试法）。**禁止按名字做职责判定；职责判定依据执行文本。**
> **② 候选全量核对=三线交叉（2026-08-24 用户定案）**：做历史候选/沉淀"全量"回搜时禁止单线定论——必须三线交叉（`book.json` 节级 yield × 读书日志逐轮清单 × 叙事链轮次摘要）后方可宣称"全量"；单线结论必须注明"仅 X 线"。

## 2. 书的登记与状态机（多会话续读）

### 2.1 入库（书首次丢入时，一次完成）

1. 原文/OCR 放入 `books/<slug>/`（工作目录，gitignore）；
2. **清洗切片**：按章切 `<章号>-<标题>.md`，每文件头注原书页码/来源；OCR 噪声处理（断行合并/页眉页脚去除/启发式错字修复，不动原意）；
3. 生成 `book.json`（见 §4 样例）：状态 `discovered`，章节全部 `unread`；
4. 生成**轻量书地图**（`读书日志.md` 开头或独立 `书地图.md`）：目录+序/结 → 各章主题一句话 + 与现有判据/原子的潜在交叉点（导航用，非沉淀）；
5. `sources/<slug>.md` 登记元数据（类型=书籍 / 位置 / 用途 / 沉淀产物清单，随读随补）。

### 2.2 状态机（宿主管状态，用户可随时改）

```
discovered ──start──▶ reading ──finish──▶ finished
                      │  ▲                 │
                      ▼  │                 ▼
                    paused ──resume─▶ reading   dropped（弃读，记录原因）
```

- 游标按节记（`cursor: ["01", …]`），**跳读天然支持**：任何时刻可指定"先读第 N 节"；
- 节状态：`unread / reading / done / skipped（用户明确跳） / revisit（待重读）`；
- 未读清单 = 非 done/skipped 的节；多书并行各持状态机，**一次会话只聚焦一本**。

## 3. 阅读轮（一次会话 = 一节/一篇，五步）

| 步 | 做什么 | 产出 |
|---|---|---|
| ① 提取 | 读当前节正文 → 观点提炼（标页码/原文位置；不省略、剥冗余） | 提取清单（每条 = 观点 + 位置 + 类型预判：原子/判据/骨架/经验/参考） |
| ② 我的看法 | 与现有库对照：**重叠 / 新信号 / 冲突点**（冲突点最值得讨论） | 对照表（每条：候选类型 + 与现有项关系） |
| ③ 你的判断 | 你圈选：**认同→沉淀 / 反对→记入反面素材 / 搁置→待重读 / 跳过** | 判定（每条 + 一句理由，由你给或我草拟你确认） |
| ④ 沉淀执行 | 认同的**当场**走四维审计入库：原子（3-run 通道）/ 判据（≥1 run 挂靠+你确认）/ 骨架条目（你确认制）；来源字段 = 书+节+页码 | 沉淀 ID 清单 + sources/ 台账更新 |
| ⑤ 下一节 | 我建议（顺序 next unread）或你指定（跳读）；更新游标/节状态 | 下一节建议 + 状态推进 |

**每轮回写**：`读书日志.md` 追加一轮（格式 §5）+ 叙事链一条（tag 书+节：做了什么/怎么看/沉淀了什么/搁置什么）。

### 3.1 上下文注入包（每次续读必带）

1. 书摘要：元信息 + 状态 + 游标 + 未读清单
2. 最近 N 轮日志摘要（上次读到哪、怎么看的、哪些被认/弃）
3. **当前节正文**（只这一节）
4. 相关现有判据/原子对照（查重用）

**明确不读**：全书全文、其他书、与本书无关的框架内容与环境噪音。

### 3.2 批量轮（batch round）

> 适用：用户指定"连读 N 节 / 一个主题段"（如"把后面相关的几节一起读了"）。与单节轮的区别只在**节奏**，五步实质不变。已在体验引擎 009-024 首次实战验证。

| 单节轮 | 批量轮 |
|---|---|
| 读 1 节 → 提取 → 看法 → 圈选 → 入库 | 读 N 节（逐节提取，节间不打断）→ **一次大梳理**（主题合成 + 与库对照 + 圈选清单）→ 一次圈选 → 批量入库 |

**批量轮规则**：
1. **提取按节**（每节 3-6 条 + 位置），不跨节混写；
2. **大梳理** = 跨节合成（重复合并、分层/系统化提炼）+ 与现有库逐点对照（重叠/新信号/冲突/自证）；
3. **圈选清单**逐项编号（①-⑩），用户可多选/合并/弃；每项标注**首源节**；
4. **入库依圈选**：`yield` 归属 = 首源节（其余相关节记 `insights` 交叉引用或留空；读书日志为全量依据）；
5. **状态推进一次性**：目标节全部 done + cursor + summary（rounds 记 1 轮，accepted 记认可项数，deferred 记搁置项数）；
6. 完成后执行 §3.3 回写清单。

### 3.3 轮后回写清单（每轮必做，顺序执行：先权威源、后派生、机检终审）

> **结算真相源（2026-08-24 定案，修复多源漂移）**：每轮"产出了什么"的**唯一真相源** = `轮次存档/<日期>-轮N.settled.json`（结构化 JSON：分节 yield + 候选四维预检结论 + summary + cursor）。读书日志 / 叙事链 / 书地图 / sources 均为**派生层**；book.json 的 yield 由 settled.json 经 advance-book.ps1 写入。**结构化抄写只发生一次**（L2 生成 settled.json 时），此后全部由机检兜底——本次漏记（038/轮10 三条：日志有、book.json 无）即"多源各自抄写"式漂移，禁止回归此模式。
> **白盒要求（2026-08-24 用户强调）**：回看时必须能回答①**来路**（这条从哪来：轮/节/L1 报告/L2 衍生）②**环节定位**（在哪个环节断的）。因此：
> - `yield.insights` 中**候选标记为必含项**（settled.json candidates 与 insights 候选条目一一对应，禁止"内容有、标记无"——本次 4 条漏标即此温床）；
> - 补录/异常修复必须带**【补录标识 + 日期 + 断点环节】**（如「【轮4 补记 2026-08-24，断点=人工抄写 yield】」），可回看时定位；

- [ ] ① L3 圈选结果 → L2 生成 `settled.json`（分节 yield 数组、候选预检合格/降级结论、summary、cursor）——唯一一次结构化抄写，生成后人工过目一遍
- [ ] ② `scripts/advance-book.ps1 -YieldFile <settled.json>` 推进 book.json（节状态 + readAt + cursor + summary + yield；**禁止手改**，手改 JSON 出过事故）
- [ ] ③ `scripts/check-book-consistency.ps1` **校验门**：book.json vs settled.json 逐字段一致——不一致=本轮未结算（修复后重跑）；**PASS 前不得宣告轮完成**
- [ ] ④ 读书日志追加本轮（全量入账；候选/沉淀计数**照 settled.json 数**；负结果也入账）
- [ ] ⑤ 叙事链回写一条（tag 书+节；批量轮 tag 主题段）——带「出发自」字段；计数引用 settled；主题段读完时提议「归档候选」（主题终了+无活跃引用，经用户确认后执行）
- [ ] ⑥ 书地图回写：交叉点判定更新（重叠 ✓ 已吸收 / 新信号 ✓ 已沉淀 / 冲突 ✗ 未发现 / 待定）+ 阅读进度（计数照 settled）
- [ ] ⑦ `sources/<slug>.md`：沉淀产物追加（照 settled 重写为当前干净态）
- [ ] ⑧ **章末核销（D5，2026-08-23）**：每章最后一节 done 的轮次，读书日志追加「本章沉淀 × 库印证核销」段（列：本章新条目 → 本书后续/已读库中的印证或冷落；结果为"冷落"的条目记入书地图待定）——回顾性自检原理在读书侧的轻量实现
- [ ] ⑨ **类型占比行（域饱和信号）**：读书日志每轮末附「新实体 / 修改 / 自证」计数——修改占比连续 >50% ⇒ 该域饱和信号（应换域/换书），占比较低 ⇒ 换域重置（增量回升）

### 3.4 并行读模式（subagent 多代理，2026-08-23 首验）

> **适用**：主题段 ≥4-5 节；或追求高效率推进。首验数据（体验引擎 025-034，10 节 / 3 代理）：**主代理上下文只增约"合并后报告"体量（每节 1-2KB），而非原文（每节 1-11KB）**——原文只在子代理上下文中，且各代理互不累积。
> **本质**：三层分工——代理读（L1）、主代理合并（L2）、用户圈选（L3）。

**L1 · 读书代理（N 个，后台 subagent）**：
- **先分组、后派发。分组决策三步**（不按序号均分）：
  1. **读概览**：书地图主题树 + 每节前 ~400 字（知道每节"讲什么"）；
  2. **划关联**：按四种关联判定找"应该放一起"的节——**同主题**（都讲 X）、**递进**（论证链/连续话题）、**互补**（原理 ↔ 案例）、**对照**（同族概念的互斥/并列/对比）；正文"连着"且话题连续者优先同组；
  3. **出分组表**：组号 / 节范围 / 关联依据（一句话）/ 组内大小——主代理先声明分组表，圈选前用户可调整。
- 每组 2-5 节（大节单独成组或配 1 小节约）；一个节只分配给一个代理；**单波组数上限 ≤5**（2026-08-23，F4：7 组并行=合并压力显著；>5 组时拆为两波，Y 波组数 ≤5）；
- 每个代理的 prompt 固定模板：①节文件绝对路径（只读这些）②提取标准（每节 3-6 条 + 位置=第N段；短文节如实说明，不硬凑）③现有库清单（判据/骨架/原子各列，含"三者区别"一句话）——**必须由 `scripts/gen-library-list.ps1` 生成，禁止手写**（手抄=漂移误判；2026-08-23 曾把 flow-channel 判据与 emotion-black-box 骨架记混）；分组表查重预判列出的现有条目，附 **2-3 行正文摘要**（不只名称+一句话）④对照判定（重叠/新信号/冲突/自证）⑤候选分类建议（骨架/判据/原子/无）⑥硬约束：**不写任何文件、不做沉淀决策、不圈选、上下文只含本任务**；⑦报告交付：**全文正文返回**（含每节四段+组总结），不得以"已在上一条消息/主响应"摘要替代（2026-08-23 教训：代理输出摘要导致 L2 拼凑）；
- **L1 报告自检清单（2026-08-23，F1）**：每代理交付前自查——①逐节提取段号齐全？②每节含对照+候选？③组总结含占比评估？④无"已在上方返回"式引用？⑤未写文件/未圈选确认？自检不过→补发完整版（教训：波 8 A 组紧凑版缺逐节四段，被迫 send_message 补发）。
- 代理完成即终（不留后台对话）。

**L1.5 · 报告落盘（2026-08-23，D2；2026-08-23 修订为"审计锚"模式）**：L1 代理仍**不写任何文件**（硬约束不变）；L2 收齐全部报告后，将各代理报告的**提取条目（含段号）+ 候选清单 + 代理 id** 存入 `books/<slug>/轮次存档/<日期>-并行读轮N.md`（审计锚模式，**不誊写全文**——全文在代理会话中，审计时 send_message 唤醒补发即可；提取条目已同步入 book.json yield，双保险）。用途=可审计，**不入注入包、不进主上下文压缩**。

**L2 · 合并层（主代理）**：
- 覆盖核对：每个代理报告的节数 vs 分配节数（防漏节）；
- 去重合并（跨代理同族候选中合并）+ 与全局库复核（判据依据/骨架引用补全）；
- 圈选清单每个候选须含：类型（骨架/判据/原子/修改）、首源节、来源代理、推荐度、**人话版解释（2-4 句，面向圈选者的原文化翻译，附书内例子）**——**第三层必须建立在用户读懂之上**（首验教训：术语压缩清单导致用户"看不懂"→补人话版后才圈选）。

**L3 · 用户圈选（统一模板，每轮固定不变）**：
- **候选编号规则**：A=新骨架、B=修改现有、C=原子候选（仅记录）、D=观察项（不入库）；每轮从 A1/B1/C1/D1 重新编号。
- **圈选清单固定五段**（L2 产出，**表格整合版**——人话版写进表格列，不另起章节；顺序与栏目不可变）：
  1. **`## ① 新沉淀候选`**：列 = 编号 / 候选 / 人话版 / 首源节 / 推荐度（★1-5，**标准：5★=结构性缺口无它不通 / 4★=强新知识 / 3★=有益补充 / 2★=细节加固 / 1★=可弃**）。人话版 2-4 句原文化翻译（附书内例子，必做不可省）。候选类型（骨架/判据/原子）写进候选名。
  2. **`## ② 修改现有`**（都是"细化/补维度"，不是新建）：列 = 编号 / 修改 / 说明（人话版+例子）/ 首源节 / 推荐度（★1-5，同上标准）
  3. **`## ③ 原子候选（仅记录，待 run 验证）`**：列 = 编号 / 候选 / 一句话 / 源节
  4. **`## ④ 观察项（不入库）`**：编号列表，供知悉的分歧/暗面/作者未处理处；**必记**——每个观察项须写入对应节 `book.json` yield.insights（未入库但有回连价值的思考，不记=丢；2026-08-23 教训：D5 等洞察仅存对话、不落库则永不回连）
  5. **`## ⑤ 书地图交叉点核对`**：列 = 待定项 / 判定（落地/仍待定/维持搁置）
- **圈选回复格式**（用户照此回复，L2 不猜）：
  - 全收 → `全圈`
  - 挑着收 → `圈：<编号列表>；弃：<编号列表>`（可含合并指令，如 `A1+A2 合并`）
  - 不收 → `全弃`
- 圈后 L2 按批量轮规则批量入库（yield 归属=首源节）。

**L4 · 默认推荐（Auto-default，2026-08-23 用户定案）**：
- **目的**：圈选清单五段信息量大，逐项审核压力高；默认推荐 = L2 给出**预授权选法**，用户只需看**差集**（默认 + 加项 / 默认 − 减项）。
- **默认推荐规则（L2 每轮附在圈选清单后，一行集合 + 一句话理由；弃项显式列出 `弃：…` + 理由，透明可翻案）**：
  1. 新沉淀候选：推荐度 **≥3★ 默认收**；≤2★ 默认不放——若与现有条目重叠且核心已被 D 观察项承载则弃（如系统思维×emergence）；
  2. 修改现有：默认收——L2 提出修改即已做内容审计与评估，用户不再逐项审；**三档必要度标注**（不做 ★）：
     - **M1 · 必要修正**：冲突修正/修订包/立场翻案（不改=库内矛盾残留）——**永远收**，必带悬空点/边界标注；
     - **M2 · 实质补强**：为既有条目补重要维度/实例（2-3 行+）——默认收；
     - **M3 · 轻量注记**：交叉引用/一句话补注——默认收；与既有描述重复时并入标注省去；
  3. 原子候选：默认全记录（仅记录不晋升，零成本）；
  4. 观察项：默认全必记（O2 条款）；
- **用户回复**：`按默认` / `默认 + A8` / `默认 - A3`（差集表达；等价于圈/弃列表）。L2 不猜；用户省略回复而明确要求默认推荐时，按默认执行并注明「本波按默认推荐执行（用户预授权）」。
- **边界条款（2026-08-23，防"沉默接受"固化）**：
  1. **悬空/冲突候选不得静默收**——候选带作者未解反例、自相矛盾或悬空点（如"奈杰尔时刻×心流通道张力"）时，即使 ★≥3 且按默认收，L2 必须显式标注"推荐收 + 下列悬空点"（悬空点入该节 yield.insights），不静默通过；
  2. **定期零默认轮**——每章或每 5 轮，强制一轮"无默认"圈选（用户真实看一遍清单再圈），防 status quo bias 固化。

### 3.5 沉淀条目的出处标注规则（2026-08-23 用户定案）

**出处只在一处标全，不重复**——各层各司其职：

| 层 | 承载 | 规则 |
|---|---|---|
| 文件头部来源行（骨架/原子） | 条目溯源锚点（可质疑时去哪翻原文） | 保留：来源 + 书 + 节 + 用户认可 |
| 判据「依据」段 | 判据无头部来源行，依据段=唯一来源承载 | 保留 |
| sources/_INDEX.md | 一源一账、查重 | 保留（摘要级） |
| book.json yield | 节级状态机 | 保留（机器层） |

**正文段落/标题/索引一句话列：不写节号**（使用者不需要知道"038"；溯源走头部/来源列）。**例外（保留）**：
1. **修改注记行**（`> 应用补充（2026-08-23，体验引擎 049/057）`）——追加说明的唯一来源标注；
2. **直接引用原文观点处**（`（066 作者原话：……）`）——引文出处；
3. **跨条目交叉引用**（`（025 已立）`——指向其他条目的来源）——库内关系标注；
4. **叙事链轮次标题**（`并行读轮 2：体验引擎 035-048`）——轮次标识，非出处复述。

**清理面**：正文/标题/一句话列中的裸节号（含"体验引擎 038"前缀式）一律删除；删节号时保留原内容与括号语义（如"（090 泛化）"→"（泛化）"）。2026-08-23 已对全库执行首轮清理。

### 3.6 并行会话协调与文档协同（2026-08-23 用户定案）

**场景**：同一本书可能被多个会话并行推进（2026-08-23 实测：轮 4 与轮 5 两路并行读同一本书，靠库清单异常才检测到）。

- **开波前核验**：派发 L1 前 `git log --oneline -3`（全局骨架仓）确认无其他会话已推进到本波范围——库清单异常是第一检测信号（轮 5 案例：清单凭空多出条目→发现并行写入）；
- **游标边界互斥**：同一本书的并行会话按游标边界分工（谁先到谁写，后者据 cursor 续读），不得改已由他波推进的节；检测到边界重叠→先核验对方质量（git 溯源+日志），再决定让位/衔接——**不盲目重读、不直接丢弃**；
- **sources/ 段每轮重写为当前干净态**（不追加"增量"段落）——防多路会话文档风格漂移（轮 5 增量式 vs 轮 6 干净式并存曾造成读错）。

**节奏选择**（同一波书内容三选一）：

| 模式 | 适用 | 主上下文代价 |
|---|---|---|
| 单节轮 | 精读难点节、用户想细聊 | 原文进主上下文（大） |
| 批量轮（串行连读+大梳理） | 4-10 节、无并行需求 | 原文进主上下文（中） |
| **并行读** | ≥4-5 节、求效率 | **报告进主上下文（最小）** |

**失败模式与防护**：漏节→L2 覆盖核对；代理跑偏（读错文件/混入判断）→ L1 模板硬约束 + 抽查引文；跨代理重复→L2 去重合并；用户看不懂→L2 人话版（必做，不可省）。

## 4. book.json 样例

```json
{
  "slug": "the-art-of-game-design",
  "title": "The Art of Game Design",
  "author": "Jesse Schell",
  "source": "epub | ocr-pdf",
  "addedAt": "2026-08-23",
  "status": "reading",
  "sections": [
    { "id": "01", "title": "第一章 体验", "pages": "1-30",
      "status": "done", "readAt": "2026-08-23",
      "yield": { "atoms": [], "verdicts": [], "skeleton": [], "insights": ["…"], "rejects": ["…"] } },
    { "id": "02", "title": "第二章 玩家", "pages": "31-60", "status": "unread", "readAt": null, "yield": {} }
  ],
  "cursor": ["02"],
  "summary": { "rounds": 1, "accepted": 0, "rejected": 1, "deferred": 0 }
}
```

## 5. 读书日志.md 记录格式（每轮）

```markdown
## [日期] · 第[N]节 [标题]（p.[范围]）
- **提取**：3-5 条观点（各标页码）
- **我的看法**：与现有库对照（重叠 X / 新信号 Y / 冲突点 Z）
- **你的判断**：认同→（沉淀 ID）/ 反对→（记反面）/ 搁置 / 跳过
- **沉淀**：原子`slug` / 判据`slug` / 骨架条目（来源=书+节+页码）
- **类型占比**：新实体 X / 修改 Y / 自证 Z（域饱和信号：修改占比连续 >50% ⇒ 换域；批量轮同此，book.json summary 同步）
- **下一节**：建议第[N+1]节（或用户指定跳读）

（若本节无沉淀：写明"本节读后无沉淀，理由：…"——负结果也入账）
```

## 6. 完成/弃读台账

- 最后一节 done → 书状态 `finished`；`sources/<slug>.md` 补完成台账（哪些章沉淀了什么 / 全书覆盖地图：读过的区域 vs 未覆盖区域——**可分析**）；
- 弃读：状态 `dropped` + 记录原因（入日志与 sources 备注），不删书。

## 7. 工具化时机

- **当前**：人工按本手册执行（规范即流程，不依赖工具自觉）；
- 试跑 5-10 轮后固化为 `harness_book_add / book_status / book_round / book_map`（G-04），面板「读书」tab（G-05）随后。

## 8. 流程回看（元优化，2026-08-23 用户定案）

> **什么**：时不时回看近期实战记录，寻找可优化点——基准=**①符合框架精神（白盒五原理/迷宫悖论/用户裁决/克制）②流程摩擦点（优化/润滑）**。
> **触发**：每读完一章（章末核销时同步）或每 5 波（取先到者）；用户主动要求时随时。
> **产出**：①回看结论（落实项/待落地项/无漂移确认）②优化项 → 认领为协议条款（本文档）或登记 FRAMEWORK-PROGRESS 待办；③已解决的演进记入叙事链（本手册改动则提交）。
> **本手册的每次修正 = 回看产物**（L4 默认推荐/M1-M3 三档/L1.5 审计锚/L1 自检清单/单波 ≤5 组——全部源自回看），形成"实战→回看→规范"正反馈。

### 本次回看（2026-08-24 · 治理轮：真相源冲突 + 原子预检门槛）

> 触发：用户提问"之前那么多波只记录了这些原子吗？"→ 三线核对（book.json/读书日志/叙事链）发现 **32 条候选 vs 26 条初查**（漏 6 条，其中 4 条 book.json 漏标：038/138/143-146/148-149）。
> **根因判定**：不是回搜方法问题，是**写入端多源漂移**——候选条目分别抄写进三条线（book.json/日志/叙事链），无单一权威源、无机检，抄写漂移不可见。

- **落实项**：① settled.json 唯一结算真相源（§3.3 ①-③，抄写一次+机检校验门，`check-book-consistency.ps1` 落地）② 原子候选四维预检前置（§1：不合格不登记；32 条历史候选全量预检——合格 16 组/降级 12/合并 3 组）③ 候选全量回搜=三线交叉（§1：禁止单线定论）
- **待落地**：settled.json 由"轮结算脚本"辅助生成（当前人工生成 + 机检校验；试跑 3 轮后考虑脚本化，登记 G-12）
- **无漂移确认**：已建 7 原子全部合格（四维体检通过）——问题集中在候选登记区，未污染既有原子库
- **叙事链**：本条随读书轮 13 回写
